# Custom WireGuard Provider Configuration
# Use this when your VPN provider is not directly supported by Gluetun,
# or when you need full control over your WireGuard configuration.
#
# Port Forwarding: ❌ Not supported (no automatic port detection)
# Protocol: WireGuard only
#
# IMPORTANT: You must have a complete WireGuard configuration from your provider

# ============================================================================
# VPN PROVIDER CONFIGURATION
# ============================================================================
# Use "custom" for any WireGuard provider not natively supported by Gluetun
VPN_SERVICE_PROVIDER=custom
VPN_TYPE=wireguard

# ============================================================================
# WIREGUARD CONFIGURATION (ALL FIELDS REQUIRED)
# ============================================================================
# Get these values from your VPN provider's WireGuard configuration file.
# The config file typically looks like:
#
#   [Interface]
#   PrivateKey = ABC123...      ← WIREGUARD_PRIVATE_KEY
#   Address = 10.2.0.2/32       ← WIREGUARD_ADDRESSES
#   DNS = 10.2.0.1
#
#   [Peer]
#   PublicKey = XYZ789...       ← WIREGUARD_PUBLIC_KEY
#   AllowedIPs = 0.0.0.0/0
#   Endpoint = 1.2.3.4:51820    ← WIREGUARD_ENDPOINT_IP:WIREGUARD_ENDPOINT_PORT

# REQUIRED: Your WireGuard private key (from [Interface] section)
WIREGUARD_PRIVATE_KEY=your_private_key_from_interface_section

# REQUIRED: Your assigned VPN IP address (from [Interface] section)
WIREGUARD_ADDRESSES=10.2.0.2/32

# REQUIRED: Server's public key (from [Peer] section)
WIREGUARD_PUBLIC_KEY=server_public_key_from_peer_section

# REQUIRED: Server IP address (from Endpoint in [Peer] section, before the colon)
WIREGUARD_ENDPOINT_IP=1.2.3.4

# REQUIRED: Server port (from Endpoint in [Peer] section, after the colon)
WIREGUARD_ENDPOINT_PORT=51820

# Optional: Preshared key (if your provider uses one)
WIREGUARD_PRESHARED_KEY=

# ============================================================================
# SERVER SELECTION - NOT USED FOR CUSTOM PROVIDER
# ============================================================================
# These settings are IGNORED when using VPN_SERVICE_PROVIDER=custom
# Leave them empty to avoid confusion
SERVER_COUNTRIES=
SERVER_CITIES=
SERVER_HOSTNAMES=

# ============================================================================
# PORT FORWARDING - NOT SUPPORTED FOR CUSTOM PROVIDER
# ============================================================================
# Automatic port forwarding is NOT available with custom providers.
# You must manually configure port forwarding with your VPN provider (if supported)
# and set QBITTORRENT_PORT to match.
VPN_PORT_FORWARDING=off
VPN_PORT_FORWARDING_PROVIDER=

# ============================================================================
# QBITTORRENT CONFIGURATION
# ============================================================================
QBITTORRENT_WEBUI_PORT=8080
QBITTORRENT_USER=admin
QBITTORRENT_PASS=change_this_password

# If your provider supports manual port forwarding, set this to your forwarded port
QBITTORRENT_PORT=6881

# ============================================================================
# FILE SYSTEM CONFIGURATION
# ============================================================================
# IMPORTANT: Use absolute paths, not ~ (tilde expansion may fail in Docker)
# macOS example: /Users/yourusername/Downloads/torrents
# Linux example: /home/yourusername/Downloads/torrents
# Windows example: C:/Users/yourusername/Downloads/torrents
DOWNLOADS_PATH=/home/username/Downloads/torrents

# User/Group IDs (run 'id -u' and 'id -g' to find your values)
PUID=1000
PGID=1000

# ============================================================================
# NETWORK CONFIGURATION
# ============================================================================
# Your local network subnet (for LAN access to Web UI)
# Find with: ip route | grep default (Linux) or netstat -nr | grep default (macOS)
LOCAL_SUBNET=192.168.1.0/24
GLUETUN_CONTROL_PORT=8000

# ============================================================================
# SYSTEM CONFIGURATION
# ============================================================================
TZ=America/Los_Angeles
LOG_LEVEL=info

# ============================================================================
# CUSTOM PROVIDER NOTES
# ============================================================================
# When to use custom provider:
# - Your VPN provider is not in Gluetun's supported list
# - You want to use a specific server endpoint
# - Built-in provider support is failing
# - You're using a self-hosted WireGuard server
#
# Requirements:
# - Complete WireGuard configuration from your provider
# - All five WireGuard fields must be filled in (private key, addresses,
#   public key, endpoint IP, endpoint port)
#
# Limitations:
# - No automatic server selection (you must specify exact endpoint)
# - No automatic port forwarding
# - No server failover (if the endpoint is down, VPN won't connect)
#
# Troubleshooting:
# - "endpoint IP is not set" → Fill in WIREGUARD_ENDPOINT_IP
# - "private key is not valid" → Check for copy/paste errors in private key
# - Connection timeout → Verify endpoint IP:PORT is correct and accessible
# - No internet after connect → Check WIREGUARD_ADDRESSES matches your config
#
# Tested with:
# - ProtonVPN (custom mode) ✅
# - Self-hosted WireGuard ✅
# - Generic WireGuard configs ✅
# ============================================================================
